// Schema defining the minimal job envelope consumed by the compiler.
// New fields must use unique numbers and preserve backward compatibility.

syntax = "proto3";

package job_proto;

option go_package = "./;job_proto";


message Job {
  string id = 1; // Unique identifier for the job.
}

